{
  "name": "college-tracker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:remote": "wrangler d1 execute repo-dashboard-work-items --remote --file=migrations/0003_college_and_okr_matrix.sql",
    "db:migrate:local": "wrangler d1 execute repo-dashboard-work-items --local --file=migrations/0003_college_and_okr_matrix.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.10"
  }
}
